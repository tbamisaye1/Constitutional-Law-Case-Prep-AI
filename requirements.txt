# Keep deps light and close to what you already use in CS Learning.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-dotenv>=1.0.0
pydantic-settings>=2.6.0
python-multipart>=0.0.12

# LLM / agents (same family as your LangChain + LangGraph courses)
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0
langgraph>=0.2.0
faiss-cpu>=1.9.0

# PDF + export
pypdf>=5.0.0
python-docx>=1.1.0
httpx>=0.27.0

# Neon Postgres. The binary extra ships prebuilt wheels, so the Vercel build
# does not need libpq or a C toolchain.
psycopg[binary]>=3.2.0
